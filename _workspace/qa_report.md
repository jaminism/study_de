# QA 결과 — 콘텐츠·앱 통합 정합성 최종 재검증 (4회차)

- 검증일: 2026-08-26
- 대상: `data/questions/*.json` (21개 카테고리 / **425문항**, 3회차 424 → +1), `app/` (Flask)
- 검증 방식: 전수 스키마 스캔 + **실제 앱 기동 후 런타임 API 호출** (`PORT=5081 python app/main.py`)
- 결과 요약: **통과 25 / 실패 0 / 경고 4**
- 3회차(FAIL 0 / WARN 6) 대비: **WARN 2건 완전 해결, 1건 성격 변경(심각 → 경미), 3건 유지.** 리더의 수동 편집 28건과 엄격화된 `keyword_matches()`로 인한 **신규 결함 0건, 회귀 0건**.
- **판정: 추가 콘텐츠 재작업 루프 불필요.** 남은 4건은 전부 "치명적이지 않은 품질 개선" 범주이며, 앱 동작·채점 정확성·보안에 영향이 없다.

---

## 재검증 우선 항목 판정

| 3회차 항목 | 판정 | 근거 |
|-----------|------|------|
| **FAIL** | ✅ **0건 유지** | 전수 스캔·런타임 검증 전 항목에서 실패 0 |
| **WARN-1** `kibana` basic 주관식 0문항 | ✅ **해결** | `kibana-sub-006` 추가 확인. (카테고리×유형×난이도) **126개 조합 전부 1문항 이상** |
| **WARN-2** 주관식 31문항 키워드 부분매칭 의존 | ⚠️ **대폭 개선, 잔여 있음** | 지목된 31문항 중 **22개 해결**. 다만 엄격 기준으로는 여전히 48문항/57키워드. **단, 3회차가 지적한 "2토큰 반쪽 매칭"은 0건으로 구조적 소멸** (아래 WARN-1) |
| **WARN-3** 지문 복붙 통과 7건 | ✅ **해결** | 지문 그대로 제출 **0/146** (정적·HTTP 양쪽). 교차 오염 9.0% → **2.87%** |
| **WARN-4** 2자 이하 키워드 5건 | ❌ **미해결** | 5건 **전부 그대로**. 다른 에이전트의 구체화 작업이 반영되지 않았다 (아래 WARN-2) |
| **WARN-5** keywords 전부 정확히 5개 | ❌ 유지 | 146문항 전부 5개 (아래 WARN-3) |
| **WARN-6** `main.py:32` 절대 임포트 | ❌ 유지 | `python -m app.main` → `ModuleNotFoundError` 재현 (아래 WARN-4) |

### 리더 실측치 독립 재현 결과

| 지표 | 리더 보고 | **QA 독립 측정** | 일치 |
|------|-----------|------------------|------|
| 모범 답안 자기 채점 만점 | 146/146 (100%) | **146/146 (100%)** — 정적·HTTP 양쪽 | ✅ |
| 문항 지문 그대로 제출 통과 | 0/146 | **0/146** — 정적·HTTP 양쪽 | ✅ |
| 같은 카테고리 교차 오염 | 2.1% | **2.87%** (28/974 순서쌍) | △ 분모 정의 차이 |
| 무의미 답안 오탐 | 0/730 | **0/730** — 정적·HTTP 양쪽 | ✅ |

교차 오염 수치 차이는 결함이 아니라 **분모 산정 방식 차이**다. 본 검증은 같은 카테고리 내 주관식 **순서쌍 전수**(974쌍, `permutations`)를 썼고, 28쌍이 오답 처리에 실패했다. 3회차의 9.0%(39/435)와 같은 방식으로 재게산해도 방향과 규모는 동일하게 **3분의 1 수준으로 감소**했다. 리더의 2.1%도 3% 미만이라는 결론에는 영향이 없다.

---

## 통과

### 콘텐츠 스키마 (1~12)

전수 스캔 결과 **스키마 위반 0건**. 리더의 수동 편집 28건(`model_answer` 문장 추가)과 키워드 3건 구체화, 신규 1문항에서 **구조적 결함이 하나도 유입되지 않았다.**

1. **JSON 유효성** — 21개 파일 전부 파싱 성공. 파싱 실패 0건. (수동 편집은 JSON 문법을 깨는 대표적 경로인데 손상 없음)
2. **ID 유일성** — 425문항 전부 고유(UNIQUE=425=TOTAL). 파일 내·파일 간 중복 0건.
3. **ID 포맷·의미 일치** — 전부 `{category}-{mcq|sub}-{NNN}`. id 접두사↔파일명, id의 `mcq`/`sub`↔`type` 필드까지 전부 일치(위반 0건). 신규 `kibana-sub-006`도 규칙 준수.
4. **category 필드 ↔ 파일명** — 425문항 전부 일치.
5. **MCQ choices 개수** — 279개 전부 정확히 4개. 분포 `{4: 279}`.
6. **answer_index 범위** — 279개 전부 0~3. 범위 초과·타입 오류·bool 혼입 0건.
7. **선택지 무결성** — 빈 선택지 0건, 한 문항 내 정규화 후 동일 선택지 0건.
8. **유형 교차 오염** — mcq에 `model_answer`/`keywords` 혼입 0건, subjective에 `choices`/`answer_index` 혼입 0건.
9. **explanation 충실성** — 425문항 전부 비어있지 않으며 40자 미만 0건.
10. **난이도** — 21개 파일 모두 basic/intermediate/advanced 보유. 전체 basic 107 / intermediate 201 / advanced 117. 파일 단위 편중(70% 초과) 0건.
11. **keywords 개수** — 146문항 전부 3~5개 규격 충족(위반 0건).
12. **문항 중복성** — 정규화 후 완전 동일한 question 텍스트 0건.

### 이전 회차 수정의 회귀 검증 (13~16)

13. **MCQ 정답 위치 분포 — 회귀 없음**

    | 정답 위치 | 3회차 | **4회차** |
    |-----------|-------|-----------|
    | index 0 (A) | 74 (26.5%) | **74 (26.5%)** |
    | index 1 (B) | 68 (24.4%) | **68 (24.4%)** |
    | index 2 (C) | 70 (25.1%) | **70 (25.1%)** |
    | index 3 (D) | 67 (24.0%) | **67 (24.0%)** |

    - **21개 파일 전원이 모든 인덱스를 15~35% 범위 내**로 유지(이탈 파일 0건). 이번 회차 편집이 주관식에 한정됐음을 뒷받침한다.

14. **`choices` 순서 ↔ `answer_index` 쌍 정합성 유지** — 무작위 60개 MCQ를 원본 JSON의 `answer_index` 그대로 HTTP 제출 → **60/60 전부 정답 처리**. API가 내려준 `choices` 배열과 원본 JSON 배열 불일치 **0건**.
15. **explanation의 선택지 번호 지목 정합성** — 선택지 번호·라벨(`1번`, `(A)`, `①`)을 지목하는 explanation **32건**(3회차와 동일 건수)을 극성(오답/정답 서술) 자동 대조한 결과 **불일치 0건**. 유일한 검출 항목 `kafka-mcq-015`는 3회차와 동일하게 "원인으로 보기 **어려운** 것"을 묻는 부정형 문항이라 정답 번호를 지목하는 것이 정상이다.
16. **선택지 라벨 상한 폴백 유지** — `result.html:74`의 `'ABCDEFGH'[loop.index0] if loop.index0 < 8 else loop.index` 폴백 코드 존속. 현재 최대 선택지 수는 4개라 발동 조건 없음.

### 콘텐츠 ↔ 앱 경계면 (17~20)

17. **필드명 완전 일치** — 콘텐츠 필드는 정확히 10종(`id, category, type, difficulty, question, explanation, choices, answer_index, model_answer, keywords`)이며 `data_loader.py`가 소비하는 집합과 일치. 조용히 무시되는 필드 0건, 콘텐츠에 없는데 읽는 필드 0건.
18. **무손실 로딩** — `/api/health` → `{"categories":21,"questions":425,"skipped":0,"status":"ok"}`. `load_errors` 0건, 기동 로그에 경고 0건. **신규 1문항 포함 425문항 전부가 앱에 도달**.
19. **카테고리 하드코딩 없음** — `data_loader.py:325` `self.directory.glob("*.json")` 디렉토리 스캔. `_LABEL_OVERRIDES`(61~83행)는 표시 라벨 전용이며 21개 카테고리 전부 커버(파일명 그대로 노출되는 카테고리 0건).
20. **집계 정확성 (API ↔ 원본 대조)** — `/api/categories`의 카테고리별 `total`·`mcq`·`subjective`·`difficulty` 4개 값을 **21개 카테고리 전부 원본 JSON 재집계와 1:1 대조**한 결과 불일치 0건. 합계 425 = 원본 425.

    | 카테고리 | total | mcq | sub | b/i/a |
    |----------|-------|-----|-----|-------|
    | clickhouse | 21 | 15 | 6 | 4/11/6 |
    | elasticsearch | 24 | 15 | 9 | 6/12/6 |
    | git | 15 | 10 | 5 | 4/7/4 |
    | hadoop | 15 | 10 | 5 | 4/7/4 |
    | hdfs | 15 | 10 | 5 | 4/7/4 |
    | hive | 21 | 14 | 7 | 4/10/7 |
    | iceberg | 15 | 10 | 5 | 4/7/4 |
    | java | 16 | 10 | 6 | 5/7/4 |
    | k8s | 15 | 10 | 5 | 4/7/4 |
    | kafka | 31 | 21 | 10 | 8/14/9 |
    | kafka-streams | 23 | 15 | 8 | 4/11/8 |
    | **kibana** | **16** | 10 | **6** | **5/7/4** |
    | python | 32 | 20 | 12 | 9/15/8 |
    | ranger | 15 | 10 | 5 | 4/7/4 |
    | rest-api | 15 | 10 | 5 | 4/7/4 |
    | scala | 24 | 16 | 8 | 6/12/6 |
    | spark | 31 | 20 | 11 | 7/15/9 |
    | spark-streaming | 22 | 14 | 8 | 5/11/6 |
    | sql | 29 | 19 | 10 | 8/13/8 |
    | trino | 15 | 10 | 5 | 4/7/4 |
    | yarn | 15 | 10 | 5 | 4/7/4 |
    | **합계** | **425** | **279** | **146** | **107/201/117** |

### 보안 / 채점 (21~23)

21. **정답 유출 차단 (조회 경로) — 회귀 없음** — `/api/questions/all?count=50` 응답 문항 키가 `{category, choices, difficulty, id, question, type}`뿐. 주관식만 요청 시 `{category, difficulty, id, question, type}`으로 **`answer_index`/`model_answer`/`keywords`/`explanation` 전부 제거 확인**.
22. **정답 유출 차단 (제출 경로) — 회귀 없음**

    | 요청 | 결과 |
    |------|------|
    | 쿠키 없이 단건 `POST /api/submit` | **403** |
    | 쿠키 없이 일괄 `POST /api/submit` | **403** |
    | 세션 보유 + 세트 외 문항 | **403** |
    | 세션 보유 + 세트 내 문항 | **200** (정상 경로 유지) |

23. **주관식 채점 — 엄격화 후에도 만점률 100% 유지, 변별력은 대폭 상승**

    | 지표 | 2회차 | 3회차 | **4회차** |
    |------|-------|-------|-----------|
    | 모범 답안 만점(1.0) | 126/141 (89.4%) | 145/145 (100%) | **146/146 (100%)** |
    | 오답 판정(<0.6) | 1 | 0 | **0** |
    | **지문 그대로 제출 통과** | — | **7** | **0** |
    | **같은 카테고리 교차 오염** | — | **9.0%** | **2.87%** (28/974) |
    | 무의미 답안 오탐 | 0 | 0 | **0** (0/730) |

    - **HTTP 경로 전수 검증**: 21개 카테고리별로 주관식 전량(146문항)에 모범 답안을 그대로 제출 → **146/146 전부 score 1.0**. 리더의 정적 측정치가 런타임에서도 그대로 재현된다.
    - **엄격화 로직이 콘텐츠와 정확히 맞물린다**: `data_loader.py:180`의 `if len(flat) >= 2 and hits < 2: return False` 가드가 걸린 문항이 하나도 없다. 즉 리더의 수동 편집 28건이 새 판정식을 **모두 만족**한다 — 콘텐츠 수정과 로직 엄격화의 순서·범위가 어긋나지 않았다.
    - **지문 복붙 방지가 실제로 작동한다**: 146문항 전부 지문을 그대로 제출해도 통과 0건. 최고 점수는 0.4(임계 0.6)로, 통과선까지 여유가 있다. 3회차에 통과하던 7문항(`sql-sub-001/002/004`, `kafka-sub-001`, `hive-sub-002`, `elasticsearch-sub-004`, `trino-sub-002`)이 **전부 0.4 이하로 하락**했다.
    - **키워드 구체화 3건 검증** — 리더가 손본 세 문항 모두 "지문으로는 못 넘고 모범 답안으로는 만점" 상태를 정확히 달성했다.

      | 문항 | 변경된 키워드 | 지문 제출 | 모범 답안 |
      |------|--------------|-----------|-----------|
      | `kafka-sub-001` | `파티션` → `파티션 단위 순서` | **0.4** (통과 실패) | **1.0** |
      | `sql-sub-001` | `행 증폭` → `행 증폭(팬 트랩)` | **0.4** | **1.0** |
      | `sql-sub-002` | `역정규화` → `역정규화(스타 스키마)` | **0.4** | **1.0** |

### API ↔ 프론트 경계면 (24~25)

24. **응답 shape 일치** — `index.html`이 읽는 `c.id/label/total/mcq/subjective/difficulty[d]`, `quiz.html`이 읽는 `q.id/category/difficulty/type/question/choices`, `result.html`이 읽는 summary 13개 필드 + 문항별 필드 전부 존재(누락 0).
    - MCQ 결과 항목 12필드: `answer_index, answered, category, choices, correct, difficulty, explanation, question, question_id, score, selected_index, type` — `result.html`이 접근하는 키 전부 포함.
    - 주관식 결과 항목 16필드: 위에 더해 `keyword_total, keywords, matched_keywords, missed_keywords, model_answer, pass_ratio, user_answer` — `result.html:84~89`이 쓰는 `matched_keywords`/`missed_keywords`/`keyword_total`/`pass_ratio` 전부 존재.
    - 실제 렌더 확인: MCQ 8문항 일괄 제출 → `/result/<id>` **HTTP 200** (17,355바이트), 주관식 6문항 일괄 제출 → **HTTP 200** (15,151바이트)에 `모범 답안`·`내 답안`·`키워드 매칭 N / M`·`kw-miss` 배지 정상 출력.
25. **에러·경계 경로** — 전부 정상.
    - **`kibana+subjective+basic`이 이제 문항을 반환한다**: API `returned=1` (`kibana-sub-006`), `/quiz` **HTTP 200**. 3회차의 유일한 공집합이 해소됐다.
    - `/api/questions/bogus` → 404, `/quiz?category=bogus` → 404, `/result/deadbeef00` → 404.
    - 잘못된 submit 본문 5종(빈 객체, 배열, `answers` 문자열, `answers:[1]`, `question_id` 누락) 전부 **400**(500 없음).
    - `count` 클램프: `999→50`, `abc→10`, `0→1`, `-5→1`.
    - 세션 쿠키 266바이트 (한계 4093).

---

## 실패 (수정 필요)

**없음.** 3회차 FAIL 0건이 유지됐고, 이번 회차의 수동 편집 28건·키워드 3건 구체화·신규 1문항·채점 로직 엄격화 어디에서도 실패 항목이 유입되지 않았다.

---

## 경고 (권장 — 전부 비차단, 추가 루프 불필요)

### WARN-1. 키워드 축자 등장률 잔여 — 48문항 / 57키워드 (3회차 WARN-2의 잔여분, **성격은 심각 → 경미로 변화**)

**담당: 각 카테고리 전문가 (선택 작업)**

3회차가 지목한 31문항 중 **22개가 해결**됐고 9개(`clickhouse-sub-003/006`, `hadoop-sub-002`, `hdfs-sub-001/005`, `iceberg-sub-002`, `java-sub-001/003`, `spark-sub-011`)가 남았다. 동시에, 3회차가 세지 않았던 39문항이 같은 엄격 기준에 새로 걸려 총 48문항/57키워드가 된다(3회차의 31문항 집계는 대상을 좁게 잡았던 것으로 보인다 — 키워드 총계는 95개 → **57개로 40% 감소**).

**다만 결함의 성격이 근본적으로 바뀌었다.** 3회차의 핵심 지적은 "2토큰 키워드가 1개 토큰만 맞아도 통과하는 반쪽 매칭"이었는데, 이 부류는 **완전히 소멸했다**:

| 잔여 키워드의 토큰 수 | 3토큰 | 4토큰 | 5토큰 | **2토큰** |
|----------------------|-------|-------|-------|-----------|
| 건수 | 32 | 24 | 1 | **0** |

| 실제 매칭된 토큰 수 | 2개 | 3개 | 4개 |
|--------------------|-----|-----|-----|
| 건수 | 33 | 23 | 1 |

즉 **최악의 경우도 3토큰 중 2토큰(67%) 일치**이며, 반쪽(50%) 매칭으로 통과하는 키워드는 하나도 없다. `data_loader.py:180`의 가드가 의도대로 그 부류를 전부 차단한 결과다.

잔여 57건의 "빠진 토큰"을 분류하면 대부분이 **의미 없는 조사/수식어**다:

- **한국어 조사 붙음 (14건)** — 키워드 쪽이 `압축률과`·`집계와`·`투명성과`·`상한은`·`수와`·`주기와` 처럼 조사가 붙은 형태라 토크나이저가 어간을 못 자른다. 모범 답안에는 어간(`압축률`, `집계`, …)이 실제로 들어 있다. **콘텐츠 결함이 아니라 키워드 표기 습관 문제**다.
  - 예: `clickhouse-sub-001` `'컬럼 지향과 단건 조회'` → 답안에 `컬럼`·`단건`·`조회`는 있고 `지향과`만 없음.
- **수식어·부사어 (약 23건)** — `대량`, `짧은`, `양쪽`, `기반`, `대신`, `방지`, `우선`, `분포`, `일관`, `과다` 등. 개념어는 전부 매칭되고 꾸밈말만 빠졌다.
- **실질 개념어 (약 20건)** — 보강 여지가 실제로 있는 것들: `iceberg-sub-002`의 `transform`, `spark-sub-007`의 `column`, `hive-sub-005`/`iceberg-sub-005`의 `비원자성`, `trino-sub-004`의 `모니터링`, `git-sub-005`의 `재클론`, `k8s-sub-004`의 `롤링`, `python-sub-003`의 `체이닝`, `hive-sub-006`의 `안정성`.

**판정: 재작업 루프 불필요.** 채점 정확도(만점 100%)·변별력(지문 통과 0, 오탐 0) 모두 목표치를 달성했고, 남은 것은 "키워드 문구를 답안 표현에 더 맞추면 좋다" 수준의 다듬기다. 여력이 있다면 위 **실질 개념어 20건만** 손대면 충분하며, 조사 14건은 키워드에서 조사를 떼는 것(`압축률과` → `압축률`)만으로 정리된다.

### WARN-2. 정규화 후 2자 이하 키워드 5건 — **3회차 대비 미해결**

**담당: batch-processing-expert, language-expert**

3회차 WARN-4로 지목된 5건이 **하나도 수정되지 않았다.** 브리핑에 "다른 에이전트가 이미 구체화 완료했는지 확인"이라 되어 있었으나, 실측 결과 전부 원문 그대로다.

| 문항 | 문제 키워드 | 현재 keywords |
|------|------------|---------------|
| `spark-sub-002` | `액션` | `['액션', 'wide dependency', '셔플 경계', '파티션당 태스크', 'DAG']` |
| `spark-sub-003` | `핫 키` | `['Summary Metrics', '핫 키', 'salting', 'AQE skew join', '브로드캐스트 조인']` |
| `spark-sub-004` | `스필` | `['기본값 200', '태스크당 100~200MB', '스필', 'small file', 'AQE coalescePartitions']` |
| `spark-sub-005` | `스큐` | `['스큐', '셔플 파티션', 'Storage Memory', 'memoryOverhead', '코어 수 축소']` |
| `scala-sub-003` | `박싱` | `['중간 컬렉션', 'Iterator/view 지연 평가', '박싱', 'mapPartitions', '가변 컬렉션 지역 사용']` |

- 위험도는 **3회차보다 낮아졌다.** 단일 토큰 키워드는 `len(flat) >= 2` 가드에 걸리지 않아 엄격화의 혜택을 못 받지만, 5개 중 1개일 뿐이라 단독으로 통과선(3/5)을 만들지 못한다. 무의미 답안 오탐 0/730이 이를 뒷받침한다.
- 다만 **교차 오염 28건 중 3건이 정확히 이 문항들에서 나온다**: `spark-sub-006 → spark-sub-003`(0.6), `spark-sub-011 → spark-sub-005`(0.6), `scala-sub-006 → scala-sub-003`(0.6). 같은 카테고리의 다른 답안이 `핫 키`·`스큐`·`박싱`을 스치듯 언급해 점수를 얻은 것으로, 잔여 오염의 실제 원인 중 하나다.
- 수정 방법: `액션` → `액션(action) 트리거`, `스큐` → `데이터 스큐`, `스필` → `디스크 스필`, `박싱` → `오토박싱 오버헤드`, `핫 키` → `핫 키 편중`처럼 **2토큰 이상**으로 구체화. 엄격화된 판정식에서 2토큰 키워드는 두 토큰 모두를 요구하므로, 수정 시 해당 모범 답안에 그 표현이 실제로 들어 있는지 함께 확인해야 한다(그렇지 않으면 만점률이 떨어진다).
- **판정: 5건뿐이고 기능 영향이 없으므로 최종 릴리스를 막을 사유는 아니다.** 다음 콘텐츠 갱신 때 함께 처리하면 충분하다.

### WARN-3. 주관식 keywords가 전부 정확히 5개 (2·3회차 유지)

- 146문항 전부 `keywords` 길이가 5(분포 `{5: 146}`). 규격 3~5개를 충족하므로 실패는 아니다.
- 핵심 개념이 3개뿐인 문항에도 5개를 채우는 관행이 WARN-1의 "답안에 없는 수식어 토큰"을 만드는 구조적 원인이다. 문항이 실제로 요구하는 개념 수에 맞춰 3~4개로 줄이면 WARN-1이 함께 완화된다.

### WARN-4. `main.py:32`의 절대 임포트 (2·3회차 유지)

- `from data_loader import ...`라 `python app/main.py` 외의 실행 방식에서 실패한다. 이번 회차에 재현 확인: `python -m app.main` → `ModuleNotFoundError: No module named 'data_loader'`.
- 실행 방식이 문서(`main.py:3`)로 고정되어 있어 우선순위는 낮다. 고치려면 `from app.data_loader import ...` + `sys.path` 폴백, 또는 `app/__init__.py` 추가.

---

## 최종 판정

- **FAIL 0건.** 4회차 연속 무결점 구간에 들어섰다.
- **차단 요소 없음.** 앱은 425문항 전부를 무손실 로드하고, 정답 유출·세션 우회·크래시 경로가 전부 막혀 있으며, 주관식 채점은 만점률 100%와 지문 복붙 차단 100%를 동시에 만족한다.
- **추가 콘텐츠 재작업 루프 불필요.** 남은 WARN 4건은 (1) 키워드 문구 다듬기, (2) 2자 키워드 5건, (3) 키워드 개수 유연화, (4) 임포트 방식으로, 모두 "있으면 더 좋은" 개선이며 사용자 경험을 해치지 않는다.
- 다음 콘텐츠 갱신 시 함께 처리할 항목: WARN-2의 5건 → WARN-1의 실질 개념어 20건 → WARN-3 순.
